/*
EJERCICIO 3 — Cambios de dirección
Dificultad: Intermedio

Implementa contarCambiosDireccion(valores).

Recibe una lista de números. Cuenta cuántas veces la tendencia cambia de subir
a bajar o de bajar a subir entre valores consecutivos.

- Los valores repetidos no cambian la tendencia y deben ignorarse.
- Si hay menos de dos cambios relevantes, devuelve 0.

Ejemplos:
contarCambiosDireccion([1, 3, 5, 2, 0, 4]) -> 2
contarCambiosDireccion([1, 1, 2, 2, 3])    -> 0
contarCambiosDireccion([5, 3, 3, 4, 1])    -> 2
contarCambiosDireccion([])                 -> 0
*/

function contarCambiosDireccion(valores) {
  // Escribe tu solución aquí.
}

