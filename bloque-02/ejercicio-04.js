/*
EJERCICIO 4 — Inventario después de movimientos
Dificultad: Intermedio+

Implementa actualizarInventario(inventario, movimientos).

Ambas listas tienen el mismo tamaño. inventario[i] es el stock inicial de un
producto y movimientos[i] puede ser positivo (entrada) o negativo (salida).
Devuelve una NUEVA lista con el stock final de cada producto.

- El stock final nunca puede ser negativo: si un cálculo da menos de 0,
  guarda 0 en esa posición.
- No modifiques inventario ni movimientos.

Ejemplos:
actualizarInventario([10, 3, 0], [-4, 5, -1]) -> [6, 8, 0]
actualizarInventario([], [])                  -> []
actualizarInventario([2, 1], [-5, -1])        -> [0, 0]
*/

function actualizarInventario(inventario, movimientos) {
  // Escribe tu solución aquí.
}

